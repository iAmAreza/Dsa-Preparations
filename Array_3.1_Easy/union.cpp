#include"bits/stdc++.h"
using namespace std;

#define ll long long 

int32_t main(){
   int n1, n2; 
   cin >> n1 >> n2; 
   vector<int>v1(n1);
   vector<int>v2(n2);
   for(int i = 0; i < n1; i++) {
   	cin >> v1[i]; 
   }
   for(int i = 0; i < n2; i++) {
   	cin >> v2[i]; 
   }
   int j = 0, k = 0; 
   while(v1.size() > j && v2.size() > k) {
   	   if(v1[j] < v2[k]) {
   	   	
   	   }
   }

   return 0; 
} 